buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

// 移除allprojects块，因为settings.gradle已经配置了repositories

task clean(type: Delete) {
    delete rootProject.buildDir
}
